This a basic header to convert a file path to work on any OS.

For example, the windows file path ("User\Folder\example.txt") would be the Unix file path ("User/Folder/example.txt") if the OS is Unix based.
